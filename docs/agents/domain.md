# Domain documentation

Layout: single-context.

Read root `CONTEXT.md` for repository-wide domain language. Record durable architectural decisions under `docs/adr/` when a hard-to-reverse decision needs an ADR. Keep terms concise and update the existing source of truth instead of duplicating definitions in multiple agent documents.
