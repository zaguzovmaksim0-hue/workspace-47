# Issue tracker

Workspace-47 uses GitHub Issues in `zaguzovmaksim0-hue/workspace-47` as the durable issue tracker. Use the `gh` CLI when issue operations are required. External pull requests are not automatically treated as the triage queue.
