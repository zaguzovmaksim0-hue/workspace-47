# ES-PUB-0158 — Diputación Provincial de Huelva — 2026-08-21

## Scope

Current bounded public pass for the Diputación Provincial de Huelva portal. No authentication, private-key signing, final filing, registration, submission, or payment was performed.

## Decisive current evidence

- `https://www.diphuelva.es/` returned HTTP 200 and currently publishes a direct **Sede Electrónica** / **Registro electrónico** link to `https://sede.diphuelva.es/`.
- `https://sede.diphuelva.es/` returned HTTP 200 and currently exposes **REG. ELECTRÓNICO / INSTANCIA GENÉRICA** (`IGE_03`).
- Following the Sede's own public `generaTicket` transition to that procedure produced the current procedure card. It states that electronic submission may be performed with an authorized digital certificate, DNI electrónico, or a Sede user/password. Session/ticket material was transient and is not retained here.
- The procedure exposes an `altaSolicitud.do` transition, but that deep link is ticket/session-bound; opening it without the public transition context returns an internal error. It is therefore not a stable start URL.

## Implementable boundary

The smallest stable contract is a QA-only navigation profile rooted at exact `https://sede.diphuelva.es/`. The public catalog may truthfully record certificate access as conditional because certificate/DNIe is one current authentication option. No `CLIENT_TLS_AUTH`, signing capability, signature format/algorithm, callback, AutoFirma contract, or final-submission contract is inferred.
