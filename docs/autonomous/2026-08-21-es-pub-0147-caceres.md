# ES-PUB-0147 — Diputación Provincial de Cáceres — Instancia General Normalizada

Reviewed: 2026-08-21

## Current official public evidence

- The current institutional portal `https://www.dip-caceres.es/` links the Diputación electronic office at `https://sede.dip-caceres.es`.
- The electronic office resolves to `/carpetaCiudadano/Init.do` and currently publishes **Instancia General Normalizada** as procedure `idproc=341`.
- The public procedure page describes a continuous electronic procedure for requests and documents addressed to Diputación units when no specific procedure or form exists.
- Its exact public `Acceder` target carries a stable non-secret procedure selector whose decoded value is `341#expgen#INSTANCIA GENERAL NORMALIZADA`.
- The target-bound login page states that general access is available through digital certificate or DNIe and separately offers **Acceso con Cl@ve**.
- In a fresh unauthenticated session, `LoginClave2Front.do` returned a form whose method is `POST`, whose exact action is `https://pasarela.clave.gob.es/Proxy2/ServiceProvider`, and whose transient field names are `SAMLRequest` and `RelayState`.
- The current sede also displays a 2026-08-13 general notice about AutoFirma 1.9 on iPhone. That portal-wide notice is not sufficient evidence for the exact signer ABI, format, algorithm or endpoint of procedure 341, so no signing contract is inferred.

## Evidence boundary and implementation consequence

The pass stopped before submitting the Cl@ve form. No SAML or RelayState value was printed or retained; no credential, certificate private key, authentication result, signature, upload, payment, registration or final submission was produced.

The evidence supports only a `QA_ONLY` navigation profile for the exact procedure page and the two observed navigation origins `sede.dip-caceres.es` and `pasarela.clave.gob.es`. The profile exposes no signing capability, client-TLS policy, signer endpoint, signature format or signature algorithm. Inventory state is therefore `IMPLEMENTED_NOT_E2E` and generated catalog state remains `E2E_PENDING`.
