# Catalunya Petició genèrica public-boundary research — generation 46 — 2026-08-11

## Scope and safety boundary

This slice used only official public, unauthenticated HTTPS GET requests against `tramits.gencat.cat`
and `ovt.gencat.cat`, plus same-origin static JavaScript. No POST or multipart form was submitted; no
secure-auth transition was activated; no credentials, certificates, cookies, signing operation,
upload, payment, administrative submission, APK launch, ADB, or device-control workflow was used.
Temporary public response bodies were deleted after extracting bounded evidence. Ephemeral session
identifiers emitted by the public service were not retained in repository evidence.

## Current procedure binding

Inventory surface `ES-PUB-0105` (`catalunya-tramits-peticio-generica`) remains `BROWSE_ONLY`.
The current public procedure page
`https://tramits.gencat.cat/ca/tramits/tramits-temes/Peticio-generica?category=72461610-a82c-11e3-a972-000c29052e2c`
returned HTTP 200 with SHA-256
`0c6a90de02f522662b68eaf0b210b43ece6de43ed635512177e2bddddb059c24`. It explicitly mentions
`certificat digital`, `idCAT Mòbil`, and `signatura electrònica`, and publishes first-party OVT links
for the generic petition.

A public GET to the linked GSITFC generic-contact frontend
`https://ovt.gencat.cat/gsitfc/AppJava/generic/conqxsGeneric.do?webFormId=31&topicLevel1.id=500&topicLevel2.id=624`
returned HTTP 200 with SHA-256
`4388c0c1220300dc73bb47b42fd594fd6fb3439a1f2021b6719e756fbff47924`. The response exposes
POST and multipart form boundaries but no AutoFirma/AutoScript/MiniApplet, signing algorithm, XAdES,
PAdES, or CAdES contract. Neither form was submitted.

The public HTML-form tramitation frontend published by the same procedure was fetched at its exact
first-party `renderitzar.do` entry for service `ING001HTM2`. It returned HTTP 200 with SHA-256
`3ac88df09cc960e7de41bdf2705a3d36462694adad3131fa1c13cc72acd3ec5e`. The response identifies
itself as `Instancia Generica Peticio generica` with mode `Peticio generica amb signatura electronica`
and tells the user to identify with a valid digital-identification mechanism such as idCAT Mòbil,
Cl@ve, DNI electrònic, idCAT Certificat, FNMT, T-CAT, or Camerfirma.

The continuation control generated by that public page targets the service's secure authentication
transition (`renderitzaruploadSecure.do`, request code `autenticarFormulariHtml`). That transition was
not activated. No session identifier from the generated URL is retained here.

## First-party GSITGF static-assets check

Five implementation-relevant same-origin JavaScript files loaded by the public tramitation frontend
were fetched and inspected:

- `scripts/v2/master.min.js`: HTTP 200, SHA-256
  `0f50ebd4f4c220ace1fb980bb6a88cf3140b0f44b3b0c8611719efbdd1506596`;
- `scripts/v2/generic_responsive.js`: HTTP 200, SHA-256
  `4312e40ef5ed5bc1f959fbb845310635a268a7d4067fda67e9713cc466fb4095`;
- `scripts/v2/master.responsive.js`: HTTP 200, SHA-256
  `62b942f7d7e7a58df8721b0441b397fde314b51b973bf28d0ee4013e176525b8`;
- `scripts/v2/sincronitza_pestanyes.js`: HTTP 200, SHA-256
  `ece96cc53ae1a480f385d0b8ad5926e7aea67179be25604897dbee8ea2d90ec9`;
- `scripts/ajax/overlay.js`: HTTP 200, SHA-256
  `d2e6cc44fcccc0f2f97d42f299ac2282b78586ece77d98fa4bef825985585fb1`.

None contains AutoFirma, AutoScript, MiniApplet, `SHA*withRSA`, XAdES, PAdES, CAdES, or an equivalent
local-signature invocation. The public pre-auth assets therefore prove the signed-procedure intent and
identification boundary, but not the browser-local signing ABI.

## Contract conclusion

The current evidence is stronger than the prior generic catalog statement: `ES-PUB-0105` is bound to
a live generic-petition frontend that explicitly declares electronic-signature mode. However, the
next state transition is authentication and the public assets before that boundary do not expose the
algorithm, signature format/mode, payload, bridge callback, result-delivery endpoint, or transport
needed for a safe `SiteProfile`.

`ES-PUB-0105` therefore remains `BROWSE_ONLY` / research-only. No profile, origin allowlist, bridge
capability, catalog binding, inventory status, or release state is changed. The next safe gate is a
first-party unauthenticated signing asset or invocation reachable before authentication or form
submission; no cryptographic value may be inferred from generic AutoFirma behavior.

## Queue impact

No new implementation-ready portal was promoted. The classified research buffer remains at least 16
surfaces. Exact implementation priority remains Sevilla ATSE after acceptable terminal Codex Cloud
evidence, preserved Melilla STA, then `extremadura-tramites` (`ES-PUB-0109`).
