# ES-PUB-0035 — CTBG — Solicitud de Información

Reviewed: 2026-08-23

## Current official public evidence

- The official CTBG Sede at `https://sede.consejodetransparencia.gob.es/` loads successfully in Chromium and exposes the public catalogue at `https://sede.consejodetransparencia.gob.es/dossier`.
- The catalogue currently lists **Solicitud de Información** at `https://sede.consejodetransparencia.gob.es/catalog/t/01b4b72b-7f21-4d7c-9576-e1d7871624a6`.
- Its **Iniciar tramitación electrónica** control points exactly to `https://sede.consejodetransparencia.gob.es/catalog/tw/01b4b72b-7f21-4d7c-9576-e1d7871624a6`.
- In a fresh public Chromium session, that exact launch reaches CTBG's **Identificación electrónica** page. The page offers Cl@ve and certificate identification and contains a `POST` form to `https://pasarela.clave.gob.es/Proxy2/ServiceProvider` with transient `SAMLRequest` and `RelayState` fields.
- The form was not submitted. No SAML values, session values, cookies or credentials were retained or committed.

## Boundary and implementation consequence

The pass stops at the first public identity boundary. No authentication, private-key operation, signing, upload, registration, final filing or payment occurred. The generic Sede links about signature systems are not promoted into a procedure-specific signing contract.

The implementation is therefore a `QA_ONLY` navigation profile for the exact **Solicitud de Información** launch. It trusts the CTBG Sede as initiator and only the observed Cl@ve service-provider origin as a redirect. It exposes no `SIGN`, `SELECT_CERTIFICATE`, `CLIENT_TLS_AUTH`, signing endpoint, signature format or algorithm. Inventory becomes `IMPLEMENTED_NOT_E2E`; generated catalog remains `E2E_PENDING`.
